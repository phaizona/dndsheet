/* Savage Worlds Adventure Edition — sheet-specific behavior */
window.Sheets.swade = {
  init() {
    // dice ratings & tracks are handled by core (die-select needs no calc, boxes are plain checkboxes)
  },
};
